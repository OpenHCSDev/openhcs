# Manuscript source moved

The sole editable source is [manuscript.md](manuscript.md).
This historical filename is a pointer, not a second manuscript.

Use `python build_paper.py build` to update the paired reading bundle.
The legacy DOCX command resolves this old filename to the canonical source.
