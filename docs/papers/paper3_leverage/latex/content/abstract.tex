We prove that the England Replication Inequality, $\Delta S_{\min}(k) - \Delta S_{\min}(1) \geq k_B \ln k$, follows from counting: a $k$-variable system has $2^k$ states, and $k \leq 2^{k-1}$ (proved by induction) yields the entropy gap via Landauer calibration. The only physics input is $k_B$. More broadly, we establish a Five-Way Equivalence: for any information-processing system $A$ with capabilities $\mathrm{Cap}(A) > 0$, the following are equivalent:
\[
\begin{aligned}
\mathrm{DOF}(A) = 1
&\;\iff\; \text{max leverage } L = |\mathrm{Cap}|/\mathrm{DOF}
\;\iff\; \text{single source of truth} \\
&\;\iff\; \mathrm{srank} = 1
\;\iff\; \text{tractable sufficiency} \\
&\;\iff\; \text{minimum thermodynamic cost}.
\end{aligned}
\]

The thermodynamic bound $E_{\mathrm{decision}} \geq \mathrm{srank} \cdot k_B T \ln 2$ is proved directly from Landauer calibration (\LH{BA7}). P~$\neq$~NP follows from physical law (\LH{LP38}: Landauer, finite energy, finite signal speed, nontrivial state space). Under these axioms the tractability equivalence holds unconditionally.

Engineering corollaries: $\mathrm{DOF} = n$ is isomorphic to a series reliability system with $n$ failure points; expected modification cost is proportional to $\mathrm{DOF}$; minimum edit-energy equals $k_B T \ln 2 \cdot \mathrm{DOF}(A)$. These follow from the equivalence.

All theorems are machine-checked in Lean~4 with no \texttt{sorry} placeholders. The only explicit physics axioms are the Second Law (non-negative entropy production) and the Thermodynamic Uncertainty Relation (Barato-Seifert 2015). An assumption ledger records all conditional dependencies.

Keywords: thermodynamics, Landauer principle, entropy production, information theory, structural rank, formal verification, degrees of freedom
