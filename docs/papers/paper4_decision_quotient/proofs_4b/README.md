# Stochastic and Sequential Regimes Lean Proofs

This directory was scaffolded by `scripts/build_papers.py scaffold paper4b`.

- Replace starter modules with your formalization.
- Run `lake build` inside this directory once dependencies are initialized.
